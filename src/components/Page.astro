---
export interface Props {
  id?: string
  extraClasses?: string[]
  children: any
}

const { id, extraClasses } = Astro.props
---

<section id={id} class:list={['page', ...(extraClasses || [])]}>
  <slot />
</section>

<style lang="scss">
  @import '../assets/styles/_index.scss';
  .page {
    display: flex;
    flex-direction: column;
    width: 100%;
    min-height: 100vh;
    transition: opacity 255ms;
    &:not(#intro) {
      padding: 8rem 1.8rem 0;
      @include bp(2) {
        padding: 8rem 4.6rem 2rem;
      }
      @include bp(3) {
        padding: 8rem 8rem 2rem;
      }
    }
  }
</style>
